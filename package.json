{
  "name": "casino-project",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "husky install",
    "lint": "eslint .",
    "lint:ci": "eslint .",
    "start": "concurrently \"npm --prefix frontend run dev\" \"npm --prefix backend start\""
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix"
    ]
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.3.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.3",
    "concurrently": "^8.2.0"
  }
}
